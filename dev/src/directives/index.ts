import type { App } from 'vue'

// ===============================
// 自定义指令注册（占位，便于后续扩展）
// 用法：app.directive('xxx', ...)
// ===============================

export function setupDirectives(_app: App) {
  // 预留：v-permission、v-debounce、v-copy 等可在此注册
}
